// Rainy Cowork - Embeddings Feature (PHASE 3)
// Embedding generation using rainy-sdk

use crate::ai::provider_types::{
    AIError, EmbeddingRequest, EmbeddingResponse, ProviderResult, TokenUsage,
};
use serde::{Deserialize, Serialize};
use std::sync::Arc;

// All embedding implementations have been removed as they were unused placeholders.
